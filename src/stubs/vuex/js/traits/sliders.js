import SlideFull from '@Slot/sliders/full'
import SlideLeft from '@Slot/sliders/left'
import SlideRight from '@Slot/sliders/right'

export default {
    data() {
        return {
            sliders: [
                SlideFull,
                SlideLeft,
                SlideRight,
            ],
        }
    },
}